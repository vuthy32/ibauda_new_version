<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.androidhive.materialdesign" >

    <!-- user permission asccess-->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"></uses-permission>
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".AppController.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MyMaterialTheme" >
        <activity
            android:name=".activity.SplaseScreen"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Main Activity-->
        <activity android:name=".activity.MainActivity"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
        </activity>

        <!-- register activity-->
        <activity
            android:name=".activity.Register"
            android:windowSoftInputMode="stateHidden"
            ></activity>
        <!-- detail activity-->
        <activity android:name=".FragmentDetial.FragmentDetailCar"
                  android:windowSoftInputMode="stateHidden">
        </activity>
        <!-- Login Activity-->
        <activity android:name=".activity.LoginActivity"
            android:windowSoftInputMode="stateHidden">
        </activity>
        <!-- Order Activity-->
        <activity android:name=".activity.OrderCarListUser"
            android:windowSoftInputMode="stateHidden">
        </activity>

        <!-- ChatUser Activity-->
        <activity android:name=".activity.ListChatUser"
            android:windowSoftInputMode="stateHidden">
        </activity>
        <!-- ChatActivtyt Activity-->
        <activity android:name=".activity.ChatActivity"
            android:windowSoftInputMode="stateHidden">
        </activity>
        <!-- SearchResult Activity-->
        <activity android:name=".activity.SearchResultActivity"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="android.app.searchable"
            android:resource="@xml/searchable" />
    </application>

</manifest>
